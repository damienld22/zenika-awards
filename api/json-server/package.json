{
    "name": "@api/json-server",
    "description": "API de démonstration utilisant json-server",
    "scripts": {
        "start": "json-server --watch ./db.json --port 3001"
    },
    "devDependencies": {
        "json-server": "^0.17.1"
    }
}
